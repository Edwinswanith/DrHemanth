{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Copy to settings.local.json (gitignored) for machine-specific overrides — extra allowed commands, personal MCP servers, etc. Do not put project-wide rules here."
}
